---
import BaseLayout from "../layouts/BaseLayout.astro";
import WaitlistForm from "../components/WaitlistForm.astro";
import ArcDial from "../components/ArcDial.astro";
---

<BaseLayout title="Qaptr — private beta for macOS">
  <section class="hero wrap">
    <div class="hero__lede">
      <h1 class="hero__headline reveal">Remember what you did without stopping to document it.</h1>
      <p class="hero__sub reveal">
        Qaptr quietly records the shape of your work on your Mac, then gives you a clear draft to review when you are ready.
      </p>

      <div class="hero__form reveal">
        <WaitlistForm variant="hero" />
      </div>
    </div>

    <figure class="pipeline reveal" aria-labelledby="pipeline-caption">
      <div class="pipeline__bar">
        <span class="pipeline__live">
          <span class="pipeline__pulse" aria-hidden="true"></span>
          Capturing
        </span>
        <span class="pipeline__interval">every 5 min</span>
      </div>

      <ol class="pipeline__stages" aria-label="How Qaptr turns a work session into a reviewable record">
        <li class="pipeline__stage">
          <span class="pipeline__node" aria-hidden="true"><ion-icon name="scan-outline"></ion-icon></span>
          <div class="pipeline__body">
            <h2 class="pipeline__title">Captures while you work</h2>
            <div class="pipeline__apps" aria-hidden="true">
              <span class="pipeline__app">Editor</span>
              <span class="pipeline__app">Browser</span>
              <span class="pipeline__app">Notes</span>
              <span class="pipeline__app pipeline__app--off">Excluded</span>
              <span class="pipeline__sweep"></span>
            </div>
          </div>
        </li>

        <li class="pipeline__stage">
          <span class="pipeline__node" aria-hidden="true"><ion-icon name="git-commit-outline"></ion-icon></span>
          <div class="pipeline__body">
            <h2 class="pipeline__title">Assembles the session for you</h2>
            <div class="pipeline__tape" aria-hidden="true">
              <div class="pipeline__event"><b>09:42</b><span>Drafted the brief</span></div>
              <div class="pipeline__event"><b>10:18</b><span>Compared source notes</span></div>
              <div class="pipeline__event"><b>11:06</b><span>Wrote the next step</span></div>
              <p class="pipeline__rollup">14 captures · assembled automatically</p>
            </div>
          </div>
        </li>

        <li class="pipeline__stage">
          <span class="pipeline__node pipeline__node--done" aria-hidden="true"><ion-icon name="checkmark-outline"></ion-icon></span>
          <div class="pipeline__body">
            <h2 class="pipeline__title">Waits for your approval</h2>
            <div class="pipeline__result" aria-hidden="true">
              <span class="pipeline__result-state">ready to review</span>
              <span class="pipeline__action">Review</span>
            </div>
          </div>
        </li>
      </ol>

      <figcaption id="pipeline-caption" class="pipeline__caption">
        <ion-icon name="lock-closed-outline" aria-hidden="true"></ion-icon>
        Example session. Nothing leaves this Mac without your approval.
      </figcaption>
    </figure>
  </section>

  <section class="dial-section blueprint-surface radiant-fluid reveal">
    <div class="wrap dial-section__inner">
      <div class="dial-section__intro">
        <p class="meta"><ion-icon name="timer-outline" aria-hidden="true"></ion-icon>Set it once</p>
        <h2>Capture at your pace.</h2>
        <p>Choose how often Qaptr checks in. Keep working. It will build the record quietly.</p>
      </div>
      <div class="dial-section__visual">
        <ArcDial />
        <p class="meta dial-section__caption">
          Set the interval from 5 seconds to 30 minutes. Qaptr captures quietly at the pace you choose.
        </p>
      </div>
    </div>
  </section>

  <section class="product-bento wrap reveal" aria-labelledby="product-bento-title">
    <div class="product-bento__intro">
      <div class="product-bento__intro-copy">
        <p class="meta">From activity to clarity</p>
        <h2 id="product-bento-title">A useful record, not a replay.</h2>
        <p>Qaptr turns the shape of your work into a short, readable timeline you can check before you report, hand off, or share.</p>
      </div>
      <div class="product-bento__intro-visual" aria-hidden="true">
        <div class="intro-visual__header">
          <span>Session record</span>
          <span>Ready to review</span>
        </div>
        <div class="intro-visual__row">
          <ion-icon name="camera-outline"></ion-icon>
          <span>Work captured</span>
          <b>09:42</b>
        </div>
        <div class="intro-visual__row">
          <ion-icon name="document-text-outline"></ion-icon>
          <span>Context organized</span>
          <b>10:18</b>
        </div>
        <div class="intro-visual__row">
          <ion-icon name="checkmark-outline"></ion-icon>
          <span>Ready to share</span>
          <b>11:06</b>
        </div>
      </div>
    </div>

    <div class="product-bento__grid">
      <article class="product-card passage product-card--capture">
        <div class="product-card__copy">
          <p class="meta"><ion-icon name="time-outline" aria-hidden="true"></ion-icon>Capture</p>
          <h3>Your work, captured as it happens.</h3>
          <p>Qaptr records the apps and windows that make up your session, so you do not have to reconstruct the day later.</p>
        </div>
        <div class="mini-timeline" aria-hidden="true">
          <span class="mini-timeline__rows">
            <span class="mini-timeline__line"></span>
            <span class="mini-timeline__row">
              <span class="mini-timeline__dot"></span>
              <span class="mini-timeline__event mini-timeline__event--one"><b>09:42</b> Outlined the brief</span>
            </span>
            <span class="mini-timeline__row">
              <span class="mini-timeline__dot"></span>
              <span class="mini-timeline__event mini-timeline__event--two"><b>10:18</b> Checked source notes</span>
            </span>
            <span class="mini-timeline__row">
              <span class="mini-timeline__dot"></span>
              <span class="mini-timeline__event mini-timeline__event--three"><b>11:06</b> Mapped next step</span>
            </span>
          </span>
        </div>
      </article>

      <article class="product-card passage product-card--privacy">
        <div class="product-card__copy">
          <p class="meta"><ion-icon name="lock-closed-outline" aria-hidden="true"></ion-icon>Privacy</p>
          <h3>Private until you say otherwise.</h3>
          <p>Your capture stays on this Mac. Nothing is shared automatically.</p>
        </div>
          <div class="privacy-orbit" aria-hidden="true">
          <span class="privacy-orbit__ring"></span>
          <span class="privacy-orbit__lock"><ion-icon name="lock-closed-outline" aria-hidden="true"></ion-icon></span>
          <span class="privacy-orbit__label">Private on this Mac</span>
          </div>
      </article>

      <article class="product-card passage product-card--document">
        <div class="product-card__copy">
          <p class="meta"><ion-icon name="document-text-outline" aria-hidden="true"></ion-icon>Review</p>
          <h3>Know what happened before you explain it.</h3>
          <p>Open a clear timeline when you need the context, not a pile of screenshots.</p>
        </div>
        <div class="mini-document" aria-hidden="true">
          <span class="mini-document__bar mini-document__bar--wide"></span>
          <span class="mini-document__bar"></span>
          <span class="mini-document__bar mini-document__bar--short"></span>
          <span class="mini-document__rule"></span>
          <span class="mini-document__tag">ready to review</span>
        </div>
      </article>

      <article class="product-card product-card--handoff">
        <div class="product-card__copy">
          <p class="meta"><ion-icon name="share-social-outline" aria-hidden="true"></ion-icon>Share</p>
          <h3>Share the result, not the whole day.</h3>
          <p>Choose the useful parts and turn them into notes for a teammate, a review, or tomorrow.</p>
        </div>
        <div class="mini-handoff" aria-hidden="true">
          <div class="mini-handoff__header">
            <span class="mini-handoff__title">Today&rsquo;s session</span>
            <span class="mini-handoff__status">ready</span>
          </div>
          <div class="mini-handoff__row"><span></span><span></span><b>Keep</b></div>
          <div class="mini-handoff__row"><span></span><span></span><b>Review</b></div>
          <div class="mini-handoff__footer">Share only what matters</div>
        </div>
      </article>
    </div>
  </section>

  <section class="privacy-lens" data-privacy-lens aria-labelledby="privacy-lens-title">
    <div class="privacy-lens__copy">
      <p class="meta"><ion-icon name="eye-off-outline" aria-hidden="true"></ion-icon>Private by default</p>
      <h2 id="privacy-lens-title">The useful part appears. The boundary stays.</h2>
      <p>Scroll to turn a captured work session into a reviewable record. Qaptr keeps the source material on your Mac until you decide what is worth sharing.</p>
      <div class="privacy-lens__status" aria-live="polite">
        <span data-privacy-state>capture held privately</span>
        <b data-privacy-progress>00%</b>
      </div>
    </div>

    <figure class="privacy-lens__visual" aria-describedby="privacy-lens-caption">
      <div class="privacy-lens__frame">
        <img src="/qaptr-workspace.webp" alt="A Qaptr workspace with a captured work session ready to review" loading="lazy" />
        <div class="privacy-lens__veil" aria-hidden="true"></div>
        <div class="privacy-lens__boundary" aria-hidden="true">
          <span>private on this Mac</span>
        </div>
        <div class="privacy-lens__record" aria-hidden="true">
          <span class="privacy-lens__record-kicker">session record</span>
          <strong>Review the shape of the work.</strong>
          <span class="privacy-lens__record-row"><i></i>Drafted the brief <b>09:42</b></span>
          <span class="privacy-lens__record-row"><i></i>Compared source notes <b>10:18</b></span>
          <span class="privacy-lens__record-row"><i></i>Wrote the next step <b>11:06</b></span>
        </div>
      </div>
      <figcaption id="privacy-lens-caption">A sample session. Scroll is the lens. Nothing is shared automatically.</figcaption>
    </figure>
  </section>

  <script is:inline>
    const privacyLens = document.querySelector("[data-privacy-lens]");
    if (privacyLens instanceof HTMLElement) {
      const state = privacyLens.querySelector("[data-privacy-state]");
      const progress = privacyLens.querySelector("[data-privacy-progress]");
      let raf = 0;
      const updatePrivacyLens = () => {
        raf = 0;
        const bounds = privacyLens.getBoundingClientRect();
        const travel = Math.max(bounds.height - window.innerHeight, 1);
        const p = Math.min(1, Math.max(0, -bounds.top / travel));
        privacyLens.style.setProperty("--privacy-p", p.toFixed(3));
        if (state) state.textContent = p < 0.32 ? "capture held privately" : p < 0.78 ? "context becoming useful" : "ready to review";
        if (progress) progress.textContent = `${String(Math.round(p * 100)).padStart(2, "0")}%`;
      };
      const requestPrivacyUpdate = () => {
        if (!raf) raf = window.requestAnimationFrame(updatePrivacyLens);
      };
      window.addEventListener("scroll", requestPrivacyUpdate, { passive: true });
      window.addEventListener("resize", requestPrivacyUpdate);
      updatePrivacyLens();
    }
  </script>

  <section id="join" class="join join--paper reveal">
    <div class="wrap join__inner">
      <div>
        <h2 class="join__headline">Get the first invite.</h2>
        <p class="join__body">Join the private beta. We will email you when your invite is ready.</p>
      </div>
      <div class="join__form">
        <WaitlistForm variant="footer" />
      </div>
    </div>
  </section>

  <style>
    /* Hallmark · genre: modern-minimal · macrostructure: Narrative Workflow · theme: Qaptr paper (locked tokens.css)
     * hero: H2 Split Diptych · enrichment: glass session panel · nav: unchanged (out of scope)
     * The workflow is stated once, in the panel. Copy on the left is headline + one sentence only;
     * each stage carries a label and an artifact, never a paragraph — the artifact is the explanation.
     * pre-emit critique: P5 H5 E5 S5 R5 V5
     * contrast: pass (40-41) · chrome: pass (47, no re-drawn window bar) · tokens: pass (48) · responsive: pass (34, 49-51)
     */
    .hero {
      position: relative;
      isolation: isolate;
      display: grid;
      gap: var(--spacing-40);
      align-items: start;
      padding-block: clamp(var(--spacing-40), 5vw, var(--spacing-64)) clamp(var(--spacing-64), 10vw, var(--spacing-96));
    }

    .hero::before {
      position: absolute;
      top: -18rem;
      right: -14rem;
      z-index: -1;
      width: min(62vw, 54rem);
      aspect-ratio: 1;
      border-radius: 50%;
      background:
        conic-gradient(from 210deg, rgb(37 99 235 / 0.16), rgb(124 58 237 / 0.12), rgb(234 88 12 / 0.16), rgb(22 163 74 / 0.12), rgb(37 99 235 / 0.16)),
        radial-gradient(circle at 42% 44%, rgb(255 255 255 / 0.7), transparent 56%);
      filter: blur(18px) saturate(125%);
      opacity: 0.86;
      transform: rotate(-18deg);
      content: "";
      pointer-events: none;
    }

    .hero::after {
      position: absolute;
      top: 8rem;
      right: 8%;
      z-index: -1;
      width: min(26vw, 20rem);
      aspect-ratio: 1;
      border: 1px solid rgb(37 99 235 / 0.18);
      border-radius: 50%;
      box-shadow: 0 0 0 1.25rem rgb(124 58 237 / 0.04), 0 0 0 3rem rgb(234 88 12 / 0.035);
      content: "";
      pointer-events: none;
    }

    .hero__lede {
      min-width: 0;
      max-width: 34rem;
    }

    .hero__headline {
      max-width: 15ch;
      min-width: 0;
      overflow-wrap: break-word;
      hyphens: none;
      color: var(--color-charcoal);
      font-size: var(--text-display);
      letter-spacing: var(--tracking-display);
      line-height: var(--leading-display);
    }

    .hero__sub {
      max-width: 44ch;
      margin-top: var(--spacing-20);
      color: var(--color-steel);
      font-size: var(--text-body-xl);
      line-height: 1.55;
    }

    .hero__form {
      max-width: 34rem;
      margin-top: var(--spacing-28);
    }

    /* Capture pipeline — the automation itself, shown once, as a quiet running process. */
    .pipeline {
      position: relative;
      min-width: 0;
      overflow: hidden;
      border: 1px solid var(--color-blue-wash);
      border-radius: var(--radius-large-card);
      background: linear-gradient(160deg, var(--color-canvas-white), var(--color-paper-mist));
      box-shadow: 0 1rem 3rem var(--color-paper-mist);
    }

    .pipeline__bar {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: var(--spacing-12);
      border-bottom: 1px solid var(--color-ash);
      padding: var(--spacing-12) var(--spacing-20);
    }

    .pipeline__live {
      display: inline-flex;
      align-items: center;
      gap: var(--spacing-8);
      min-width: 0;
      color: var(--color-green-ink);
      font-family: var(--font-geist-mono);
      font-size: var(--text-caption);
      letter-spacing: var(--tracking-meta);
      line-height: 1;
      text-transform: uppercase;
    }

    .pipeline__pulse {
      flex: 0 0 auto;
      width: 7px;
      height: 7px;
      border-radius: 50%;
      background: var(--color-vivid-green);
    }

    .js-motion .pipeline__pulse {
      animation: pipeline-pulse 2400ms var(--ease-in-out) infinite;
    }

    @keyframes pipeline-pulse {
      0%,
      100% {
        opacity: 1;
        transform: scale(1);
      }

      50% {
        opacity: 0.3;
        transform: scale(0.82);
      }
    }

    .pipeline__interval {
      color: var(--color-fog);
      font-family: var(--font-geist-mono);
      font-size: var(--text-caption);
      letter-spacing: var(--tracking-meta);
      line-height: 1;
      white-space: nowrap;
    }

    .pipeline__stages {
      margin: 0;
      padding: var(--spacing-24) var(--spacing-20) var(--spacing-16);
      list-style: none;
    }

    .pipeline__stage {
      position: relative;
      display: grid;
      grid-template-columns: 1.75rem minmax(0, 1fr);
      gap: var(--spacing-16);
      padding-bottom: var(--spacing-20);
    }

    /* The connector that makes three steps read as one automatic flow. */
    .pipeline__stage::before {
      position: absolute;
      top: 1.75rem;
      bottom: 0.25rem;
      left: 0.875rem;
      width: 1px;
      background: linear-gradient(var(--color-smoke), var(--color-ash));
      content: "";
    }

    .pipeline__stage:last-child {
      padding-bottom: 0;
    }

    .pipeline__stage:last-child::before {
      display: none;
    }

    .pipeline__node {
      display: grid;
      width: 1.75rem;
      height: 1.75rem;
      place-items: center;
      border: 1px solid var(--color-blue-wash);
      border-radius: 50%;
      background: var(--color-canvas-white);
      color: var(--color-electric-blue);
    }

    .pipeline__node ion-icon {
      font-size: 0.9rem;
    }

    .pipeline__node--done {
      border-color: var(--color-vivid-green);
      background: var(--color-soft-mint);
      color: var(--color-green-ink);
    }

    .pipeline__body {
      min-width: 0;
    }

    .pipeline__title {
      color: var(--color-charcoal);
      font-size: var(--text-body-lg);
      font-weight: 600;
      line-height: 1.3;
    }

    .pipeline__apps {
      position: relative;
      display: flex;
      flex-wrap: wrap;
      gap: var(--spacing-8);
      overflow: hidden;
      margin-top: var(--spacing-12);
      border-radius: var(--radius-input);
    }

    .pipeline__app {
      border: 1px solid var(--color-ash);
      border-radius: var(--radius-tag);
      background: var(--color-canvas-white);
      padding: var(--spacing-4) var(--spacing-12);
      color: var(--color-slate);
      font-family: var(--font-geist-mono);
      font-size: var(--text-caption);
    }

    .pipeline__app--off {
      border-style: dashed;
      border-color: var(--color-smoke);
      background: transparent;
      color: var(--color-fog);
      text-decoration: line-through;
      text-decoration-color: var(--color-smoke);
    }

    .pipeline__sweep {
      position: absolute;
      inset-block: 0;
      left: 0;
      width: 40%;
      background: linear-gradient(90deg, transparent, rgb(37 99 235 / 0.1), transparent);
      pointer-events: none;
    }

    .js-motion .pipeline__sweep {
      animation: pipeline-sweep 4200ms var(--ease-in-out) infinite;
    }

    @keyframes pipeline-sweep {
      0% {
        transform: translateX(-110%);
      }

      100% {
        transform: translateX(330%);
      }
    }

    .pipeline__tape {
      margin-top: var(--spacing-12);
      border: 1px solid var(--color-ash);
      border-radius: var(--radius-card);
      background: var(--color-canvas-white);
      padding: var(--spacing-4) var(--spacing-12) var(--spacing-8);
    }

    .pipeline__event {
      display: grid;
      grid-template-columns: 3.25rem minmax(0, 1fr);
      gap: clamp(var(--spacing-8), 1.5vw, var(--spacing-12));
      align-items: baseline;
      padding-block: var(--spacing-8);
    }

    .pipeline__event + .pipeline__event {
      border-top: 1px solid var(--color-ash);
    }

    .pipeline__event b {
      color: var(--color-fog);
      font-family: var(--font-geist-mono);
      font-size: var(--text-caption);
      font-variant-numeric: tabular-nums;
      font-weight: 400;
    }

    .pipeline__event span {
      min-width: 0;
      color: var(--color-charcoal);
      font-size: var(--text-body);
      line-height: 1.4;
    }

    .pipeline__rollup {
      margin-top: var(--spacing-4);
      border-top: 1px solid var(--color-ash);
      padding-top: var(--spacing-8);
      color: var(--color-deep-sapphire);
      font-family: var(--font-geist-mono);
      font-size: var(--text-caption);
      letter-spacing: var(--tracking-meta);
    }

    .pipeline__result {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      justify-content: space-between;
      gap: var(--spacing-12);
      margin-top: var(--spacing-12);
      border: 1px solid var(--color-vivid-green);
      border-radius: var(--radius-card);
      background: var(--color-green-wash);
      padding: var(--spacing-12) var(--spacing-16);
    }

    .pipeline__result-state {
      color: var(--color-green-ink);
      font-family: var(--font-geist-mono);
      font-size: var(--text-caption);
      letter-spacing: var(--tracking-meta);
      text-transform: uppercase;
    }

    .pipeline__action {
      border: 1px solid var(--color-primary-action-fill);
      border-radius: var(--radius-button);
      background: var(--color-primary-action-fill);
      padding: var(--spacing-4) var(--spacing-12);
      color: var(--color-canvas-white);
      font-size: var(--text-caption);
      font-weight: 600;
    }

    /* No max-width here: the rule above the caption should span the whole panel. */
    .pipeline__caption {
      display: flex;
      align-items: center;
      gap: var(--spacing-8);
      border-top: 1px solid var(--color-ash);
      padding: var(--spacing-12) var(--spacing-20);
      color: var(--color-steel);
      font-size: var(--text-caption);
      line-height: 1.5;
    }

    .pipeline__caption ion-icon {
      flex: 0 0 auto;
      color: var(--color-green-ink);
      font-size: 0.9rem;
    }

    @media (max-width: 360px) {
      .pipeline__bar {
        align-items: flex-start;
        flex-direction: column;
        gap: var(--spacing-8);
      }
    }

    @media (prefers-reduced-motion: reduce) {
      .js-motion .pipeline__pulse,
      .js-motion .pipeline__sweep {
        animation: none;
      }

      .pipeline__sweep {
        display: none;
      }
    }

    .dial-section {
      border-top: 1px solid var(--color-ash);
      border-bottom: 1px solid var(--color-ash);
    }

    .dial-section__inner {
      display: grid;
      gap: var(--spacing-32);
      align-items: center;
      padding-block: clamp(var(--spacing-48), 8vw, var(--spacing-80));
    }

    .dial-section__intro {
      max-width: 28rem;
    }

    .dial-section h2 {
      margin-top: var(--spacing-8);
      color: var(--color-charcoal);
      font-size: var(--text-heading-lg);
      line-height: var(--leading-headline);
      letter-spacing: -0.02em;
    }

    .dial-section__intro > p:last-child {
      margin-top: var(--spacing-12);
      color: var(--color-steel);
    }

    .dial-section__visual {
      min-width: 0;
    }

    .dial-section__caption {
      max-width: 48ch;
      margin: var(--spacing-12) auto 0;
      text-align: center;
    }

    .product-bento {
      display: grid;
      gap: clamp(var(--spacing-24), 4vw, var(--spacing-40));
      padding-block: clamp(var(--spacing-64), 10vw, var(--spacing-112));
    }

    .product-bento__intro {
      display: grid;
      grid-template-columns: minmax(0, 0.85fr) minmax(18rem, 1.15fr);
      gap: clamp(var(--spacing-24), 5vw, var(--spacing-64));
      align-items: center;
      border: 1px solid var(--color-charcoal);
      border-radius: var(--radius-large-card);
      background: var(--color-charcoal);
      padding: clamp(var(--spacing-24), 4vw, var(--spacing-40));
      color: var(--color-canvas-white);
    }

    .product-bento__intro-copy,
    .product-card__copy { min-width: 0; }
    .product-bento__intro .meta { color: var(--color-smoke); }
    .product-bento__intro h2 { max-width: 16ch; margin-top: var(--spacing-12); color: var(--color-canvas-white); font-size: clamp(2rem, 4vw, 3.25rem); letter-spacing: -0.03em; line-height: 1.04; }
    .product-bento__intro-copy > p:last-child { max-width: 36ch; margin-top: var(--spacing-16); color: var(--color-smoke); font-size: var(--text-body-xl); }

    .product-bento__intro-visual {
      display: grid;
      gap: var(--spacing-12);
      align-content: center;
      min-width: 0;
      border: 1px solid var(--color-slate);
      border-radius: var(--radius-card);
      background: var(--color-midnight-ink);
      padding: var(--spacing-20);
    }

    .intro-visual__header,
    .intro-visual__row { display: grid; grid-template-columns: minmax(0, 1fr) auto; gap: var(--spacing-12); align-items: center; min-height: 2rem; }
    .intro-visual__header { color: var(--color-silver); font-family: var(--font-geist-mono); font-size: var(--text-caption); letter-spacing: var(--tracking-meta); text-transform: uppercase; }
    .intro-visual__header span:last-child { color: var(--color-vivid-green); }
    .intro-visual__row { grid-template-columns: 1rem minmax(0, 1fr) auto; border-top: 1px solid var(--color-graphite); padding-top: var(--spacing-12); color: var(--color-canvas-white); }
    .intro-visual__row ion-icon { color: var(--color-silver); font-size: 1rem; }
    .intro-visual__row b { color: var(--color-silver); font-family: var(--font-geist-mono); font-size: var(--text-caption); font-weight: 400; }

    .product-bento__grid {
      display: grid;
      border-top: 1px solid var(--color-ash);
    }

    .product-card {
      display: grid;
      grid-template-columns: minmax(10rem, 0.7fr) minmax(0, 1.3fr);
      gap: clamp(var(--spacing-24), 7vw, var(--spacing-112));
      align-items: center;
      min-width: 0;
      border: 0;
      border-bottom: 1px solid var(--color-ash);
      border-radius: 0;
      background: transparent;
      padding-block: clamp(var(--spacing-32), 6vw, var(--spacing-64));
    }

    .product-card--privacy { background: var(--color-canvas-white); }
    .product-card--privacy .product-card__copy { padding-left: 0; }
    .product-card--privacy .meta { color: var(--color-vivid-green); }

    .product-card__copy h3 { max-width: 16ch; margin-top: var(--spacing-12); color: var(--color-charcoal); font-family: var(--font-satoshi); font-size: clamp(1.6rem, 2.6vw, 2.25rem); font-weight: 500; letter-spacing: -0.025em; line-height: 1.06; }
    .product-card__copy > p:last-child { max-width: 38ch; margin-top: var(--spacing-16); color: var(--color-steel); }

    .mini-timeline,
    .privacy-orbit,
    .mini-document,
    .mini-handoff {
      width: 100%;
      min-height: 11rem;
      border: 1px solid var(--color-ash);
      border-radius: var(--radius-input);
      background: var(--color-canvas-white);
    }

    .mini-timeline { --timeline-axis-gutter: 2.5rem; display: grid; align-content: center; min-height: 12rem; padding: var(--spacing-20); }
    .mini-timeline__rows { position: relative; display: grid; grid-template-columns: var(--timeline-axis-gutter) 1fr; grid-template-rows: repeat(3, 2.25rem); gap: var(--spacing-12); align-items: center; }
    .mini-timeline__line { position: absolute; top: 1.125rem; bottom: 1.125rem; left: calc(var(--timeline-axis-gutter) / 2); width: 1px; background: var(--color-smoke); transform: translateX(-50%); }
    .mini-timeline__row { display: contents; }
    .mini-timeline__dot { position: relative; z-index: 1; grid-column: 1; justify-self: center; width: 9px; height: 9px; border: 2px solid var(--color-canvas-white); border-radius: 50%; background: var(--color-charcoal); }
    .mini-timeline__event { position: relative; z-index: 1; grid-column: 2; display: flex; align-items: center; min-height: 2.25rem; border-radius: var(--radius-input); background: var(--color-paper-mist); padding: var(--spacing-8) var(--spacing-12); color: var(--color-steel); font-size: var(--text-caption); line-height: 1.35; white-space: nowrap; }
    .mini-timeline__event b { margin-right: var(--spacing-8); color: var(--color-charcoal); font-family: var(--font-geist-mono); font-weight: 500; }

    .privacy-orbit { position: relative; display: grid; min-height: 12rem; place-items: center; overflow: hidden; }
    .privacy-orbit__ring { position: absolute; width: 10rem; height: 10rem; border: 1px solid var(--color-smoke); border-radius: 50%; }
    .privacy-orbit__ring::after { position: absolute; inset: 1.125rem; border: 1px dashed var(--color-vivid-green); border-radius: 50%; content: ""; }
    .privacy-orbit__lock { position: relative; display: grid; width: 4rem; height: 4rem; place-items: center; border: 1px solid var(--color-soft-mint); border-radius: 50%; background: var(--color-canvas-white); color: var(--color-vivid-green); font-size: 2.1rem; box-shadow: var(--shadow-subtle); }
    .privacy-orbit__label { position: absolute; right: var(--spacing-16); bottom: var(--spacing-12); color: var(--color-vivid-green); font-family: var(--font-geist-mono); font-size: var(--text-caption); letter-spacing: var(--tracking-meta); text-transform: uppercase; }

    .mini-document { display: grid; grid-template-rows: auto auto auto 1fr auto; align-content: center; gap: var(--spacing-12); min-height: 12rem; padding: var(--spacing-24); }
    .mini-document__bar { display: block; width: 72%; height: 8px; border-radius: var(--radius-tag); background: var(--color-smoke); }
    .mini-document__bar--wide { width: 88%; height: 14px; background: var(--color-charcoal); }
    .mini-document__bar--short { width: 48%; }
    .mini-document__rule { display: block; align-self: end; width: 100%; height: 1px; margin-block: var(--spacing-4); background: var(--color-ash); }
    .mini-document__tag { justify-self: start; color: var(--color-steel); font-family: var(--font-geist-mono); font-size: var(--text-caption); text-transform: uppercase; }

    .mini-handoff { display: grid; gap: var(--spacing-12); min-height: 12rem; padding: var(--spacing-20); }
    .mini-handoff__header,
    .mini-handoff__row { display: flex; align-items: center; justify-content: space-between; gap: var(--spacing-12); }
    .mini-handoff__title { color: var(--color-charcoal); font-weight: 600; }
    .mini-handoff__status { color: var(--color-steel); font-family: var(--font-geist-mono); font-size: var(--text-caption); text-transform: uppercase; }
    .mini-handoff__row { display: grid; grid-template-columns: 1rem minmax(0, 1fr) auto; justify-content: initial; border-top: 1px solid var(--color-paper-mist); padding-top: var(--spacing-8); color: var(--color-steel); font-size: var(--text-caption); }
    .mini-handoff__row span:first-child { display: block; width: 0.75rem; height: 0.75rem; border: 1px solid var(--color-smoke); border-radius: 50%; }
    .mini-handoff__row span:nth-child(2) { min-width: 0; height: 7px; align-self: center; background: var(--color-smoke); }
    .mini-handoff__row b { font-weight: 500; }
    .mini-handoff__footer { margin-top: auto; color: var(--color-steel); font-family: var(--font-geist-mono); font-size: var(--text-caption); letter-spacing: var(--tracking-meta); text-transform: uppercase; }

    .join { border-top: 1px solid var(--color-ash); background: var(--color-paper-mist); }
    .join__inner { display: grid; gap: var(--spacing-24); align-items: center; padding-block: clamp(var(--spacing-48), 8vw, var(--spacing-80)); }
    .join__headline { margin-top: var(--spacing-8); color: var(--color-charcoal); font-family: var(--font-satoshi); font-size: clamp(2rem, 5vw, 3rem); font-weight: 500; letter-spacing: -0.025em; line-height: 1.08; }
    .join__body { margin-top: var(--spacing-12); color: var(--color-steel); }
    .join__form { min-width: 0; }
    .join :global(.waitlist-form) { max-width: 40rem; }

    .privacy-lens {
      --privacy-p: 0;
      display: grid;
      grid-template-columns: minmax(0, 0.7fr) minmax(0, 1.3fr);
      gap: clamp(var(--spacing-32), 8vw, var(--spacing-112));
      align-items: center;
      min-height: 170vh;
      border-top: 1px solid var(--color-ash);
      background: var(--color-midnight-ink);
      color: var(--color-canvas-white);
      padding: clamp(var(--spacing-64), 10vw, var(--spacing-112)) var(--spacing-16);
    }

    .privacy-lens__copy {
      position: sticky;
      top: 28vh;
      align-self: start;
      max-width: 30rem;
      padding-left: max(0px, calc((100vw - 75rem) / 2));
    }

    .privacy-lens__copy .meta { color: var(--color-vivid-green); }
    .privacy-lens__copy h2 { max-width: 12ch; margin-top: var(--spacing-12); color: var(--color-canvas-white); font-family: var(--font-satoshi); font-size: clamp(2.25rem, 5vw, 4.8rem); font-weight: 500; letter-spacing: -0.045em; line-height: 0.98; }
    .privacy-lens__copy > p:not(.meta) { max-width: 34ch; margin-top: var(--spacing-24); color: var(--color-smoke); font-size: var(--text-body-xl); }
    .privacy-lens__status { display: flex; justify-content: space-between; gap: var(--spacing-16); max-width: 24rem; margin-top: clamp(var(--spacing-32), 5vw, var(--spacing-64)); border-top: 1px solid var(--color-slate); padding-top: var(--spacing-12); color: var(--color-smoke); font-family: var(--font-geist-mono); font-size: var(--text-caption); letter-spacing: var(--tracking-meta); text-transform: uppercase; }
    .privacy-lens__status b { color: var(--color-vivid-green); font-weight: 400; }

    .privacy-lens__visual { position: sticky; top: 12vh; align-self: start; width: min(100%, 56rem); margin: 0; }
    .privacy-lens__frame { position: relative; overflow: hidden; aspect-ratio: 1200 / 751; border: 1px solid var(--color-slate); background: var(--color-graphite); box-shadow: 0 2rem 5rem rgb(0 0 0 / 0.38); }
    .privacy-lens__frame img { width: 100%; height: 100%; object-fit: cover; opacity: calc(0.42 + var(--privacy-p) * 0.58); filter: saturate(calc(0.45 + var(--privacy-p) * 0.55)); transform: scale(calc(1.04 - var(--privacy-p) * 0.04)); }
    .privacy-lens__veil { position: absolute; inset: 0; background: rgb(10 10 10 / calc(0.54 - var(--privacy-p) * 0.44)); mix-blend-mode: multiply; pointer-events: none; }
    .privacy-lens__boundary { position: absolute; inset: 10% auto 10% 8%; width: 1px; background: var(--color-vivid-green); transform: translateX(calc(var(--privacy-p) * 52vw)); transition: transform 120ms linear; }
    .privacy-lens__boundary span { position: absolute; top: -1.5rem; left: 0.75rem; width: max-content; color: var(--color-vivid-green); font-family: var(--font-geist-mono); font-size: var(--text-caption); letter-spacing: var(--tracking-meta); text-transform: uppercase; }
    .privacy-lens__record { position: absolute; right: 5%; bottom: 8%; display: grid; gap: var(--spacing-8); width: min(78%, 24rem); border: 1px solid rgb(255 255 255 / 0.38); background: rgb(10 10 10 / calc(0.94 - var(--privacy-p) * 0.12)); padding: clamp(var(--spacing-16), 3vw, var(--spacing-24)); transform: translateY(calc((1 - var(--privacy-p)) * 2.5rem)); }
    .privacy-lens__record-kicker { color: #86efac; font-family: var(--font-geist-mono); font-size: var(--text-caption); letter-spacing: var(--tracking-meta); text-transform: uppercase; }
    .privacy-lens__record strong { max-width: 18ch; color: var(--color-canvas-white); font-family: var(--font-satoshi); font-size: clamp(1.35rem, 2.5vw, 2.1rem); font-weight: 500; letter-spacing: -0.025em; line-height: 1.02; }
    .privacy-lens__record-row { display: grid; grid-template-columns: 0.6rem minmax(0, 1fr) auto; gap: var(--spacing-8); align-items: center; border-top: 1px solid rgb(255 255 255 / 0.24); padding-top: var(--spacing-8); color: #f5f5f5; font-size: var(--text-caption); }
    .privacy-lens__record-row i { width: 0.45rem; height: 0.45rem; border-radius: 50%; background: var(--color-vivid-green); }
    .privacy-lens__record-row b { color: #d4d4d4; font-family: var(--font-geist-mono); font-size: 0.65rem; font-weight: 400; }
    .privacy-lens__visual figcaption { margin-top: var(--spacing-12); color: #a3a3a3; font-family: var(--font-geist-mono); font-size: var(--text-caption); letter-spacing: var(--tracking-meta); text-transform: uppercase; }

    @media (max-width: 767px) {
      .privacy-lens { grid-template-columns: 1fr; gap: var(--spacing-48); min-height: 150vh; padding-block: var(--spacing-64); }
      .privacy-lens__copy { position: relative; top: auto; padding-left: 0; }
      .privacy-lens__visual { top: 18vh; }
      .privacy-lens__boundary { transform: translateX(calc(var(--privacy-p) * 86vw)); }
    }

    @media (prefers-reduced-motion: reduce) {
      .privacy-lens__frame img,
      .privacy-lens__boundary,
      .privacy-lens__record { transition: none; }
    }

    @media (min-width: 1000px) {
      .hero { grid-template-columns: minmax(22rem, 1fr) minmax(26rem, 1fr); gap: clamp(var(--spacing-40), 5vw, var(--spacing-64)); align-items: center; }
    }

    @media (min-width: 768px) {
      .dial-section__inner,
      .join__inner { grid-template-columns: minmax(0, 0.8fr) minmax(0, 1.2fr); }

      .product-card:nth-child(odd) .product-card__copy { grid-column: 2; grid-row: 1; }
      .product-card:nth-child(odd) > .mini-timeline,
      .product-card:nth-child(odd) > .privacy-orbit,
      .product-card:nth-child(odd) > .mini-document,
      .product-card:nth-child(odd) > .mini-handoff { grid-column: 1; grid-row: 1; }
    }

    @media (max-width: 767px) {
      .product-bento { gap: var(--spacing-40); padding-block: var(--spacing-64); }
      .product-bento__intro { grid-template-columns: 1fr; gap: var(--spacing-24); padding: var(--spacing-20); }
      .product-card { grid-template-columns: 1fr; gap: var(--spacing-20); padding-block: var(--spacing-32); }
      .product-bento__grid { row-gap: var(--spacing-32); border-top: 0; }
      .product-card--privacy .product-card__copy { padding-left: 0; }
      .product-card__copy h3 { max-width: 18ch; font-size: clamp(1.5rem, 7vw, 1.9rem); }
      .product-card__copy > p:last-child { margin-top: var(--spacing-12); }
      .mini-timeline,
      .privacy-orbit,
      .mini-document,
      .mini-handoff { min-height: 9.5rem; }
      .mini-timeline { --timeline-axis-gutter: 2rem; min-height: 10.5rem; padding: var(--spacing-16); }
      .privacy-orbit { min-height: 10.5rem; }
      .privacy-orbit__ring { width: 8rem; height: 8rem; }
      .privacy-orbit__lock { width: 3.25rem; height: 3.25rem; font-size: 1.8rem; }
      .privacy-orbit__label { right: var(--spacing-8); bottom: var(--spacing-8); }
      .mini-document { min-height: 10.5rem; padding: var(--spacing-20); }
      .mini-handoff { min-height: 10.5rem; padding: var(--spacing-16); }
    }
  </style>
</BaseLayout>
